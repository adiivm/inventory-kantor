# Bind to the LAN interface
bind-interfaces
listen-address=172.17.7.70

# Resolve inventory.lan to the host's LAN IP
address=/inventory.lan/172.17.7.70

# Forward other queries to public DNS
server=8.8.8.8
server=1.1.1.1

# Don't read /etc/hosts or /etc/resolv.conf
no-hosts
no-resolv
